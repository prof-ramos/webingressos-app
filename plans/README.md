# Implementation Plans

Generated by the `improve` skill on 2026-08-03. The repository is a Next.js 16 +
Supabase foundation. Execute these plans in order unless a dependency note says
otherwise. Each executor must read the entire plan, run its verification gates,
and update the status row when the plan is complete.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|--------|
| 001 | Establish an application verification baseline | P1 | L | — | DONE | [#5](https://github.com/prof-ramos/webingressos-app/issues/5) |
| 002 | Restrict audit-log creation to trusted database operations | P1 | M | 001 | DONE | [#6](https://github.com/prof-ramos/webingressos-app/issues/6) |
| 003 | Remove order PII from the general authenticated surface | P1 | M | 001 | DONE | [#7](https://github.com/prof-ramos/webingressos-app/issues/7) |
| 004 | Enforce the event lifecycle and status history | P1 | L | 001 | TODO | [#8](https://github.com/prof-ramos/webingressos-app/issues/8) |
| 005 | Enforce ledger approval and payment invariants | P1 | M | 001, 002 | TODO | [#9](https://github.com/prof-ramos/webingressos-app/issues/9) |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with a one-line
reason) | `REJECTED` (with a one-line rationale).

## Dependency notes

- Plans 002–005 depend on 001 because each changes a security-sensitive database
  boundary and must land with a repeatable application/database verification path.
- Plan 005 also depends on 002 because approval and payment transitions must use
  the trusted audit-writing boundary established there.
- These plans add migrations; do not edit any migration that is already present.
  The remote development database is documented as having the current migrations
  applied, so every schema change must be a new migration and must be tested on a
  disposable local database before any separately authorized remote push.

## Findings considered and deferred

- Commercial cross-row integrity (`orders.event_id`, promoter/event links, and
  ticket/order relationships): high-confidence and important, but not selected in
  this batch. It should follow Plan 004 and be planned as a separate migration so
  its invariants do not get mixed with the event state machine.
- Login redirect preservation, logout, recovery, and invitation flows: concrete
  product gaps documented in `CLAUDE.md:245-252`, but deferred until the database
  boundaries and verification baseline are in place.
- Missing security headers and small client-side fetch optimizations: no
  high-confidence, high-leverage fix was selected while the application has no
  real business-data screens.
